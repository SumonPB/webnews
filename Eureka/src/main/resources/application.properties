# ✅ CORRECTO: Render asigna el puerto automáticamente via $PORT
server.port=${PORT}

# Nombre de la aplicación
spring.application.name=eureka-server

# DESHABILITAR COMPLETAMENTE el cliente Eureka
eureka.client.register-with-eureka=false
eureka.client.fetch-registry=false

# Configuración CRÍTICA - usa el mismo puerto dinámico
eureka.client.service-url.defaultZone=http://${eureka.instance.hostname}:${server.port}/eureka/
eureka.instance.hostname=eureka-2okv.onrender.com

# Configuración del servidor Eureka
eureka.server.enable-self-preservation=false
eureka.server.wait-time-in-ms-when-sync-empty=0

# Para Render - importante
eureka.instance.non-secure-port=${server.port}